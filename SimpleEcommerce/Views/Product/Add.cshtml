@model ProductModel
@{
}

<div class="container">
    <div class="row"></div>
    <div class="row" style="margin-top:75px;">
        <form asp-action="Add" asp-controller="Product" method="post">
             <div asp-validation-summary="All" class="text-danger"></div>
  <div class="form-group">
    <label asp-for="@Model.Name"></label>
    <input class="form-control" asp-for="@Model.Name">
  </div>
  <div class="form-group">
     <label asp-for="@Model.Price"></label>
    <input class="form-control" asp-for="@Model.Price">
  </div>
  <div class="form-group">
     <label asp-for="@Model.Tax"></label>
    <input class="form-control" asp-for="@Model.Tax">
  </div>

   <div class="form-group">
     <label asp-for="@Model.CategoryID">Kategori seçimi</label>
      <select asp-for="@Model.CategoryID">

                <option disabled selected>Lütfen bir kategori seçiniz.</option>
                @foreach (var category in @Model.Categories)
                {
                    <option value="@category.CategoryId">@category.Name</option>
                }

      </select>
  </div>
        <button class="btn btn-primary" type="submit">submit</button>
    </form>
    </div>
    <div class="row"></div>

</div>
